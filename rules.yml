rules:
  allow:
    - enabled: true
      order: 1
      operations: [FETCH]
      providers: [github]
      slugs: ["/richardmcsong/sample"]

    - enabled: true
      order: 2
      operations: [PUSH, FETCH]
      providers: [github]
      owners: [coopernetes]
